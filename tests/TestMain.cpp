/*
 * doctest's entry point.
 *
 * Kept in a translation unit of its own so the framework implementation is compiled
 * once rather than in every test file.
 */
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "vendor/doctest.h"
